{
  "title": "Full Helchang Fortune detail UI reading redesign",
  "summary": "Rebuilt /fortune/today as a single reading flow (hero ? energy ? base ? story ? luck ? guide ? data ? apply ? mission ? quote). Presentation-only; fortune engine/API untouched. Birth fields shown via existing /users/me.",
  "git": { "branch": "main", "commit": "PENDING" },
  "changedFiles": [
    "frontend/src/components/fortune/FortuneDashboard.tsx",
    "frontend/src/components/fortune/reading/*",
    "frontend/src/components/fortune/FortuneTraditionalDetail.tsx",
    "frontend/src/components/fortune/fortuneVisuals.ts",
    "frontend/src/pages/fortune/FortuneDetailPage.tsx",
    "frontend/src/styles/fortune-reading.css",
    "frontend/src/i18n/locales/*/fortune.json"
  ],
  "testFocus": [
    "First viewport shows core theme + stars + one-liner",
    "Sections follow reading IA without duplicate old bundles",
    "Data section empty-state when no logs; fortune still rich",
    "Apply section separates fortune vs data",
    "Traditional detail accordion expands pillars/cycles",
    "No backend/shared fortune math changes in this commit"
  ],
  "fastChecks": [
    "cd frontend && npx tsc -p tsconfig.json --noEmit",
    "node scripts/i18n-audit.mjs --sync",
    "rg -n \"fr-page|FortuneReadingHero|sectionApply\" frontend/src/components/fortune frontend/src/styles/fortune-reading.css",
    "git diff --stat HEAD~1 -- shared/src/fortune backend/server/services/fortune || true"
  ],
  "productionChecks": [
    "After Pages deploy: open /fortune/today and walk sections on mobile width"
  ],
  "deploy": {
    "expected": "GitHub Pages Deploy Frontend success (FE-only)"
  },
  "asIs": "Mixed old/new cards, scores before story, data not in flow",
  "toBe": "One reading narrative from hero to mission with unified section shells"
}
