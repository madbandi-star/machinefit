{
  "title": "Wire Google OAuth client id for Pages + docs",
  "summary": "Frontend deploy workflow embeds VITE_GOOGLE_CLIENT_ID; render.yaml documents GOOGLE_CLIENT_ID; local .env updated (not committed). Migration 091 auth_providers applied on prod DB.",
  "git": { "branch": "main", "commit": "pending" },
  "changedFiles": [
    ".github/workflows/frontend-deploy.yml",
    "render.yaml",
    "docs/DEPLOY.md",
    "config/env/deployment-secrets.example"
  ],
  "testFocus": "Login page Google button; after Render GOOGLE_CLIENT_ID set, complete Google sign-in.",
  "fastChecks": [
    "npm run test:smoke:changed"
  ],
  "productionChecks": [
    "Ctrl+F5 after Pages deploy",
    "Render Environment has GOOGLE_CLIENT_ID then Manual Deploy",
    "Google Cloud OAuth JS origins include https://madbandi-star.github.io and local Vite origin",
    "Login with Google creates/links account"
  ],
  "deploy": {
    "frontend": "GitHub Pages via push to main",
    "backendDeployNeeded": true,
    "note": "Set GOOGLE_CLIENT_ID on Render if not already present, then redeploy API"
  },
  "asIs": "Social Google button showed not-configured; auth_providers missing on DB.",
  "toBe": "Frontend builds with Google client id; DB ready; Render still needs GOOGLE_CLIENT_ID env for token verify."
}
