{
  "title": "Fix Render backend tsc",
  "summary": "Remove unused banner userId, drop duplicate assert import, verify OIDC nonce on JWT payload (jose v5 has no nonce option), exclude tests from backend tsc.",
  "git": { "branch": "main", "commit": "pending" },
  "changedFiles": [
    "backend/server/services/banner.service.ts",
    "backend/server/controllers/banner.controller.ts",
    "backend/server/utils/oauth-verify.util.ts",
    "backend/server/services/usage-limit-decision.test.ts",
    "backend/tsconfig.json"
  ],
  "testFocus": [
    "Render backend build success",
    "OAuth nonce still rejected when missing/mismatched"
  ],
  "fastChecks": [
    "npx tsc --noEmit -p backend/tsconfig.json"
  ],
  "productionChecks": [
    "Render deploy for this commit is live"
  ],
  "deploy": { "expected": "Push main; Render BE rebuild" },
  "asIs": "Render tsc exit 2",
  "toBe": "Backend build succeeds"
}
