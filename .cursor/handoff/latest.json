{
  "title": "Fix system restore jsonb array bind error",
  "summary": "Admin full restore failed with invalid input syntax for type json while upserting workout_logs. node-pg was binding JS arrays as Postgres arrays into jsonb columns; restore now JSON.stringifies json/jsonb object values and surfaces the real error message.",
  "git": {
    "branch": "main",
    "commit": "PENDING"
  },
  "changedFiles": [
    "backend/server/services/system-backup.service.ts"
  ],
  "testFocus": [
    "Admin system backup ZIP ? full restore with YES succeeds",
    "Failed job error_message no longer invalid input syntax for type json on workout_logs"
  ],
  "fastChecks": [
    "rg -n \"bindRowValues|loadJsonColumns|JSON.stringify\" backend/server/services/system-backup.service.ts"
  ],
  "productionChecks": [
    "After Render redeploy: Admin ? ??? ?? ? ?? ?? succeeds"
  ],
  "deploy": {
    "expected": "Push main ? Render backend redeploy"
  },
  "asIs": "System restore failed at workout_logs jsonb array columns (progress ~70).",
  "toBe": "json/jsonb values serialized correctly; restore completes."
}
