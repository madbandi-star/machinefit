{
  "title": "Sentry production smoke verified (FE+BE)",
  "summary": "Sent intentional smoke events: frontend via ingest (accepted), backend via temporary /ops/sentry-smoke (HTTP 500) then removed the route.",
  "git": {
    "branch": "main",
    "commit": "pending"
  },
  "changedFiles": [
    "backend/server/routes/ops.routes.ts",
    ".cursor/handoff/latest.json",
    ".cursor/handoff/LATEST_TEST_HANDOFF.md"
  ],
  "testFocus": [
    "Sentry machinefit-frontend Issues shows frontend smoke",
    "Sentry machinefit-backend Issues shows backend smoke",
    "GET /api/v1/ops/sentry-smoke is gone (404)"
  ],
  "fastChecks": [
    "rg -n \"sentry-smoke\" backend/server/routes/ops.routes.ts; echo exit:$?"
  ],
  "productionChecks": [
    "Confirm both Sentry projects have smoke issues",
    "curl smoke URL should 404 after cleanup deploy"
  ],
  "deploy": {
    "expected": "Push removes smoke route; Render redeploy"
  },
  "asIs": "DSN secrets set; live capture unproven",
  "toBe": "FE+BE smoke events sent; temporary smoke route removed"
}
