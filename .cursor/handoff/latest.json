{
  "title": "No today card when completing future workout sets",
  "summary": "Workout log upsert no longer mirrors recent_history (viewed_at=NOW) for non-today logDate, so completing incomplete?complete on a future Records card does not spawn a today card.",
  "git": { "branch": "main", "commit": "dfffaf03" },
  "changedFiles": [
    "backend/server/services/workout-log.service.ts"
  ],
  "testFocus": [
    "Future-date plan card: tap complete on a set ? no new today individual card",
    "Future card stays on its date; set completion persists",
    "Today card set complete still works / still appears in Records"
  ],
  "fastChecks": [
    "rg -n \"logDate === todayDateKey\\(\\)\" backend/server/services/workout-log.service.ts",
    "rg -n \"historyRepository.record\" backend/server/services/workout-log.service.ts"
  ],
  "productionChecks": [
    "After Render BE deploy: Records future card complete ? no today duplicate"
  ],
  "deploy": { "expected": "Render BE required; Pages FE unchanged" },
  "asIs": "Completing a set on a future date card creates/moves a history card under today",
  "toBe": "Completing a future (or past) log does not bump recent_history to today; only today saves mirror history"
}
