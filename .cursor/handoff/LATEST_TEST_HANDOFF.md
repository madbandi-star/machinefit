# Test handoff ? Rollback Complete glow dot

## Summary
??? `[??]` ?? ?? ? ??. ?? ?? + ?? ??? ?? pulse? ??.

## Git
- branch: `main`
- commit: PENDING

## Changed files
- `frontend/src/styles/recommendation.css`

## Test focus
1. ???? ?? ? ??
2. ?? ?? / ?? ?? ?? pulse
3. ??? ??

## Fast checks
```bash
rg -n "complete-btn--pending::after|workout-complete-dot-glow|workout-complete-nudge" frontend/src/styles/recommendation.css
```

## as-is ? to-be
- **as-is:** ??? ?? ?? ?
- **to-be:** ? ??, ?? ???
